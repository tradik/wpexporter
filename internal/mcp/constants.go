package mcp

// schemaTypeString is the JSON Schema "string" type name used across tool input
// schemas. Defined once to avoid repeating the literal (DRY / goconst).
const schemaTypeString = "string"
