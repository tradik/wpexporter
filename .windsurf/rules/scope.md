---
trigger: always_on
---

1. application that scan wordpress wp api to download all content and image with videos from a website. if content is not listed try brute force to guess id and expiort all content.

2. the goal is to create json or markdown output

3. keep monorepo repository

4. second application should be able to get a content using xmlrpc . make whole manual for that command. 

5. when exporting markdown please include links for images

6. export need to recreate a website structure as categories, each post should be as a seperate file. make categories as folders.

7. default output should be export/{domain-name}.{date}{time} unless user specify other.

8. exported posts/pages json/markdown does not need date in a file name.

